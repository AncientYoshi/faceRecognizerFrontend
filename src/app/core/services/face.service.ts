import { HttpClient } from '@angular/common/http';import { Injectable, inject } from '@angular/core';import { Observable, of, switchMap } from 'rxjs';import { environment } from '../../../environments/environment';import { FaceRegistration } from '../models/api.models';import { AuthService } from './auth.service';import { StudentService } from './student.service';
@Injectable({providedIn:'root'})export class FaceService{private http=inject(HttpClient);private auth=inject(AuthService);private student=inject(StudentService);get():Observable<FaceRegistration>{if(this.auth.isPreview())return of({id:'preview-face',studentId:'preview-student',studentNumber:'STU-24018',embeddingId:'preview-embedding',registeredAt:new Date().toISOString(),updatedAt:new Date().toISOString()});return this.student.getMyProfile().pipe(switchMap(p=>this.http.get<FaceRegistration>(`${environment.apiUrl}/students/${p.studentId}/face`)))}register(image:File):Observable<FaceRegistration>{if(this.auth.isPreview())return this.get();const body=new FormData();body.append('image',image,image.name);return this.student.getMyProfile().pipe(switchMap(p=>this.http.post<FaceRegistration>(`${environment.apiUrl}/students/${p.studentId}/face/register`,body)))}}
