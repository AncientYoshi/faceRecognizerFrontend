import { Component, OnDestroy, OnInit, inject, signal } from '@angular/core';import { DatePipe } from '@angular/common';import { StudentService } from '../../../core/services/student.service';import { FaceService } from '../../../core/services/face.service';import { FaceRegistration } from '../../../core/models/api.models';import { IconComponent } from '../../../shared/components/icon/icon.component';
@Component({selector:'app-student-profile',standalone:true,imports:[DatePipe,IconComponent],templateUrl:'./student-profile.component.html',styleUrl:'./student-profile.component.css'})export class StudentProfileComponent implements OnInit,OnDestroy{readonly student=inject(StudentService);private face=inject(FaceService);readonly registration=signal<FaceRegistration|null>(null);readonly selected=signal<File|null>(null);readonly preview=signal('');readonly saving=signal(false);readonly message=signal('');ngOnInit(){this.student.getMyProfile().subscribe();this.face.get().subscribe({next:x=>this.registration.set(x),error:e=>{if(e.status!==404)this.message.set(e.error?.message||'Could not load face registration status.')}})}choose(e:Event){const file=(e.target as HTMLInputElement).files?.[0];if(!file)return;if(!file.type.startsWith('image/')){this.message.set('Choose a JPEG, PNG, or WebP image.');return}if(this.preview())URL.revokeObjectURL(this.preview());this.selected.set(file);this.preview.set(URL.createObjectURL(file));this.message.set('')}register(){const file=this.selected();if(!file)return;this.saving.set(true);this.face.register(file).subscribe({next:x=>{this.registration.set(x);this.saving.set(false);this.message.set('Face registered successfully.');this.selected.set(null)},error:e=>{this.saving.set(false);this.message.set(e.error?.message||'Face registration failed.')}})}ngOnDestroy(){if(this.preview())URL.revokeObjectURL(this.preview())}}
