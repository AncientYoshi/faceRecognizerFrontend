import { Component, OnInit, inject, signal } from '@angular/core';import { ActivatedRoute, RouterLink } from '@angular/router';import { DatePipe, DecimalPipe } from '@angular/common';import { DashboardService } from '../../core/services/dashboard.service';import { AcademicService } from '../../core/services/academic.service';import { Enrollment, TeacherDashboard } from '../../core/models/api.models';import { IconComponent } from '../../shared/components/icon/icon.component';
@Component({selector:'app-teacher-portal',standalone:true,imports:[RouterLink,DatePipe,DecimalPipe,IconComponent],templateUrl:'./teacher-portal.component.html',styleUrl:'./teacher-portal.component.css'})export class TeacherPortalComponent implements OnInit{private route=inject(ActivatedRoute);private dashboards=inject(DashboardService);private academics=inject(AcademicService);readonly mode=this.route.snapshot.data['mode'] as 'dashboard'|'courses';readonly data=signal<TeacherDashboard|null>(null);readonly enrollments=signal<Enrollment[]>([]);readonly selectedCourse=signal('');readonly loading=signal(true);readonly error=signal('');ngOnInit(){this.dashboards.getTeacherDashboard().subscribe({next:x=>{this.data.set(x);this.loading.set(false);if(this.mode==='courses'&&x.myCourses.length)this.select(x.myCourses[0].courseId)},error:e=>{this.error.set(e.error?.message||'Could not load the teacher portal.');this.loading.set(false)}})}select(courseId:string){this.selectedCourse.set(courseId);this.academics.enrollments(courseId,0,100).subscribe({next:x=>this.enrollments.set(x.content),error:e=>this.error.set(e.error?.message||'Could not load course students.')})}}
